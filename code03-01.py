katok = []

def add_data(friend):
    katok.append(None)
    klen = len(katok)
    katok[klen-1] = friend

add_data('다현')
print(katok)
add_data('정연')
add_data('쯔위')
add_data('사나')
add_data('지효')
add_data('모모')
print(katok)

